{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "gunicorn app:app --workers 2 --timeout 300 --bind 0.0.0.0:8080",
    "restartPolicyType": "ON_FAILURE"
  }
}
